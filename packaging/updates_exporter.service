[Unit]
After=network.target

[Service]
Type=simple
User=root
Group=root
ExecStart=/usr/local/bin/updates-exporter --config /etc/updates_exporter/config.yaml
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
